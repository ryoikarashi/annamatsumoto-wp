const base = 'Anna Matsumoto (松本杏菜)';
const spacer = ' - ';
const appender = spacer + base;

export const titles = {
  appender,
  top: base,
  me: `ME${appender}`,
  works: `WORKS${appender}`,
  notFound: `Not Found :(${appender}`
};
